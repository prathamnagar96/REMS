version: '3.8'

services:
  api:
    build:
      context: ./server
    env_file:
      - ./server/.env
    ports:
      - "8000:8000"

  client:
    build:
      context: ./client
      args:
        VITE_API_BASE_URL: http://localhost:8000
    ports:
      - "5173:80"
    depends_on:
      - api
